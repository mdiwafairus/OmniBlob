# ==============================================================================
# File Sync & Object Storage Service - Environment Template
# Copy this file to .env or use these variables in Systemd / Docker
# ==============================================================================

# Server Configuration
PORT=8080
HOST=0.0.0.0

# PostgreSQL Database Credentials
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_database_password_here
DB_NAME=peduliwni
DB_SSLMODE=disable

# Storage Configuration (Optional Overrides)
# STORAGE_ROOT_PATH=./data/storage
# STORAGE_LEGACY_PATH=./data/legacy_uploads
